# ---- Client (Vite) ----
# Safe to expose; the anon key is protected by Row Level Security.
VITE_SUPABASE_URL=https://chjwumixiaazupjznggc.supabase.co
VITE_SUPABASE_ANON_KEY=<publishable-key>

# ---- Supabase Edge Functions (set via `supabase secrets set`) ----
# SUPABASE_URL and SUPABASE_SERVICE_ROLE_KEY are injected by the platform.
CLIP_SPACE_ID=Lucitrippin/litter-ally-clip
CLIP_SERVICE_SECRET=<shared-secret-also-set-on-the-Space>
# Optional: attributes ZeroGPU quota to your account (5 min/day) rather than
# the unauthenticated pool (2 min/day).
HF_TOKEN=<hf-read-token>

# ---- Hugging Face Space secret ----
# CLIP_SERVICE_SECRET=<same-shared-secret>
